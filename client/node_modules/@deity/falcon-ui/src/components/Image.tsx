import { themed } from '../theme';

export type ImageProps = Parameters<typeof Image>[0];
export const Image = themed({
  tag: 'img',

  defaultTheme: {
    image: {
      css: {
        objectFit: 'contain',
        maxWidth: '100%'
      }
    }
  }
});
