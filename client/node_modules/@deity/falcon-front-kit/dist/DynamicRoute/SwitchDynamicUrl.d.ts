import React from 'react';
import { match as Match, SwitchProps } from 'react-router-dom';
import { Location } from 'history';
export declare type SwitchDynamicURLProps = SwitchProps & {
    /**
     * invoked when fetching `Url` data
     * @example
     * <SwitchDynamicURL
        onLoading={({ component }) => (
          <React.Fragment>
            <Loader variant="overlay" />
            {component}
          </React.Fragment>
        )}
      >
     */
    onLoading?: (props: {
        component: React.ReactNode;
        location?: Location;
        match: Match<any>;
    }) => React.ReactNode;
};
/**
 * `react-router` `Switch` component which is able to handle dynamically resolved components.
 * It works with `Url` query.
 * @example
 * <SwitchDynamicURL>
    <Route exact path="/" component={Home} />
    <Route exact type="shop-product" component={Product} />
    <p>not Found</p>
  </SwitchDynamicURL>
 * @param {SwitchDynamicURLProps} props
 */
export declare const SwitchDynamicURL: React.FC<SwitchDynamicURLProps>;
//# sourceMappingURL=SwitchDynamicUrl.d.ts.map