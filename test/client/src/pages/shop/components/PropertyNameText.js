import React from 'react';
import { Text } from '@deity/falcon-ui';

export const PropertyNameText = props => (
  <Text variant="caps" color="secondaryText" fontSize="xxs" fontWeight="bold" {...props} />
);
